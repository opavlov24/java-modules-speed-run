plugins {
    id 'application'
}

dependencies {
    implementation project(":api")

    runtimeOnly project(":api-netty")
}

application {
    mainModule = 'engage.module.app'
    mainClass = 'io.falcon.engage.app.EntryPoint'
}